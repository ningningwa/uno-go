package play;

public class Main {
    public static void main(String[] args) {
        /*you can set your args by using the tool or set it here. (args are player names) */
        Game game = new Game();
        game.start(args, null);
    }
}
